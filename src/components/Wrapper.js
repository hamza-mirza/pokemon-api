/**
 * @file Wrapper.js
 * Simple container component used for consistent styling across the application.
 * Wraps children components with a predefined set of CSS styles.
 */

const Wrapper = ({ children }) => <div className="wrapper">{children}</div>

export default Wrapper
